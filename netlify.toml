[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# No redirect rules needed: this is a single page (no client-side routing),
# and /data/*.json plus /.netlify/functions/* are served directly by default.
