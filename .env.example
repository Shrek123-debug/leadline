# Copy this file to .env for local testing with `netlify dev` (optional).
# In production, set this as an environment variable in the Netlify
# dashboard instead: Site settings -> Environment variables.
# NEVER commit a real key to this repo.

ANTHROPIC_API_KEY=your-key-here
